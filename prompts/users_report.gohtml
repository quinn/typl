<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Report</title>
</head>
<body>
    <h1>User Report</h1>

    <p>Report generated on: {{.GenerationDate}}</p>

    <h2>Active Users</h2>
    <table>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Email</th>
            <th>Role</th>
            <th>Status</th>
        </tr>
        {{range .Users}}
        <tr>
            <td>{{.ID}}</td>
            <td>{{.FirstName}} {{.LastName}}</td>
            <td>{{.Email}}</td>
            <td>{{.Role}}</td>
            <td>
                {{if .IsActive}}
                    <span style="color: green;">Active</span>
                {{else}}
                    <span style="color: red;">Inactive</span>
                {{end}}
            </td>
        </tr>
        {{end}}
    </table>

    <h2>Statistics</h2>
    <ul>
        <li>Total Users: {{.TotalUsers}}</li>
        <li>Active Users: {{.ActiveUsers}}</li>
        <li>Inactive Users: {{.InactiveUsers}}</li>
    </ul>

    {{if .HasPremiumUsers}}
    <h2>Premium Users</h2>
    <ul>
        {{range .PremiumUsers}}
        <li>{{.FirstName}} {{.LastName}} (Member since: {{.MemberSince}})</li>
        {{end}}
    </ul>
    {{else}}
    <p>No premium users at this time.</p>
    {{end}}

    <footer>
        <p>Generated by {{.GeneratedBy}}</p>
    </footer>
</body>
</html>